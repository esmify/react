{
  "name": "root",
  "private": true,
  "scripts": {
    "version": "lerna exec npm version",
    "build": "lerna run build",
    "publish": "lerna publish from-package --contents pkg"
  },
  "devDependencies": {
    "lerna": "^3.13.3"
  }
}
