{
  "name": "root",
  "private": true,
  "scripts": {
    "version": "lerna exec npm version",
    "build": "lerna run build",
    "release": "lerna publish --contents pkg"
  },
  "devDependencies": {
    "lerna": "^3.13.3"
  }
}
